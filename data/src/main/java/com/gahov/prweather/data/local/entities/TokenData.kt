package com.gahov.prweather.data.local.entities

/**
 * A data class representing token data, typically containing an access token.
 *
 * @property accessToken The access token associated with the token data.
 */

data class TokenData(
    val accessToken: String
)